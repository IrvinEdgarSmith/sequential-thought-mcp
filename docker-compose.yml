version: '3.8'

services:
  sequential-thought-mcp:
    build: .
    container_name: sequential-thought-mcp
    ports:
      - "8000:8000"
    restart: unless-stopped
    networks:
      - mcp-network

networks:
  mcp-network:
    driver: bridge
